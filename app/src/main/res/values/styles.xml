<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/onyxGrey</item>
        <item name="colorPrimaryDark">@color/eerieBlack</item>
        <item name="colorAccent">@color/crimson</item>
        <item name="android:textColorPrimary">@android:color/primary_text_dark_nodisable</item>
        <item name="android:textColorPrimaryInverse">@android:color/primary_text_light_nodisable</item>
        <item name="android:navigationBarColor">@color/onyxGrey</item>
        <item name="android:colorBackground">@color/silver</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@android:transition/slide_bottom</item>
        <item name="android:windowExitTransition">@android:transition/slide_top</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorPrimaryInverse">@android:color/primary_text_dark_nodisable
        </item>
        <item name="android:navigationBarColor">@color/onyxGrey</item>
        <item name="android:colorBackground">@color/silver</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
