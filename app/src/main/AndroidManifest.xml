<?xml version="1.0" encoding="utf-8"?><!-- suppress CheckTagEmptyBody -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tech.holm.vinabynabsyncforvikings">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:networkSecurityConfig="@xml/network_security_config"

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AllAccountsActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.OldAccountDetailsActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.TransactionsActivity" />
        <activity android:name=".activities.AccountDetailsActivity"></activity>
    </application>

</manifest>